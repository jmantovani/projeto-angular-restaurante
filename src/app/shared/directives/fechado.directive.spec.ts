import { FechadoDirective } from './fechado.directive';

describe('FechadoDirective', () => {
  it('should create an instance', () => {
    const directive = new FechadoDirective();
    expect(directive).toBeTruthy();
  });
});
