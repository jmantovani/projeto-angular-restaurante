const url = 'http://localhost:3000/';

export const environment = {
  production: true,
  api: {
    retirada: url + 'retirada',
    entrega: url + 'retirada',
    favoritos: url + 'favoritos',
    restaurantes: url + 'restaurantes'
  }
};
